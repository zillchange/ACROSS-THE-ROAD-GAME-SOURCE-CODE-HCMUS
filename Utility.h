#pragma once
#include <string>
#include <random>
using namespace std;

string getTime();
bool isHaveIntersection(int aStart, int aEnd, int bStart, int bEnd);
int getRandomInt(int min, int max);